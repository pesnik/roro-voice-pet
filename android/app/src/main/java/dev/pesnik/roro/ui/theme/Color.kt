package dev.pesnik.roro.ui.theme

import androidx.compose.ui.graphics.Color

// Sampled directly from the calico pet sprite (calico_happy.webp) rather than
// picked from a generic Material palette — keeps the in-app chat visually
// consistent with the floating pet overlay.
val CalicoOrange = Color(0xFFF5985C)
val CalicoOrangeDeep = Color(0xFF6B3D1F)
val CalicoCream = Color(0xFFF5E6D8)
val CalicoParchment = Color(0xFFFBE7D2)
val CalicoBrownDark = Color(0xFF1F120B)
val CalicoBrownSurface = Color(0xFF241509)
val CalicoBrownContainer = Color(0xFF3A2417)
val CalicoBrownOutline = Color(0xFF6B4A32)
val CalicoPink = Color(0xFFE8918A)
val CalicoTextOnOrange = Color(0xFF2B1710)
